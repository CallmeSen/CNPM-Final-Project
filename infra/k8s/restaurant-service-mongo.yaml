apiVersion: v1
kind: Service
metadata:
  name: mongodb-restaurant
  namespace: cnpm-food-delivery
spec:
  selector:
    app: mongodb-restaurant
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-restaurant
  namespace: cnpm-food-delivery
spec:
  serviceName: mongodb-restaurant
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-restaurant
  template:
    metadata:
      labels:
        app: mongodb-restaurant
    spec:
      containers:
        - name: mongodb
          image: mongo:latest
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: "admin"
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: "admin123"
            - name: MONGO_INITDB_DATABASE
              value: "Restaurant"
          volumeMounts:
            - name: mongodb-restaurant-data
              mountPath: /data/db
            - name: init-script
              mountPath: /docker-entrypoint-initdb.d
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: init-script
          configMap:
            name: mongodb-restaurant-init
  volumeClaimTemplates:
    - metadata:
        name: mongodb-restaurant-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-restaurant-init
  namespace: cnpm-food-delivery
data:
  init-restaurant.js: |
    db = db.getSiblingDB('Restaurant');
    db.createCollection('restaurants');
    db.createCollection('categories');
    db.createCollection('foodItems');
    print('Restaurant database initialized');
