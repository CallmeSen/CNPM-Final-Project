apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: cnpm-food-delivery
  labels:
    app: order-service
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: your-dockerhub-username/cnpm-order-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5005
          name: http
        env:
        - name: PORT
          value: "5005"
        - name: NODE_ENV
          value: "production"
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: cnpm-secrets
              key: MONGODB_URI
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: cnpm-secrets
              key: JWT_SECRET
        livenessProbe:
          httpGet:
            path: /health
            port: 5005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5005
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cnpm-food-delivery
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
  - port: 5005
    targetPort: 5005
    protocol: TCP
    name: http
